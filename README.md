# futfinder-ia

Comandos 

Crear ambiente: python -m venv .venv
Activa ambiente: .venv/Scripts/activate

Copiar dependencias: pip freeze > requirements.txt 
Instalar dependencias: pip install -r requirements.txt

streamlit run proyecto.py